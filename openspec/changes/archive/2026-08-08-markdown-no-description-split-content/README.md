# markdown-no-description-split-content

Remove the description field from markdown items across the data model and form; the markdown item form hides Description, lets Content fill the remaining space, and splits the content input into two synchronized textareas (editor left, viewer right).
