## Implementation Tasks

- [x] Review the existing SpecKit feature directories under `specs/` and identify the ones to preserve as historical artifacts.
- [x] Create `specs/legacy-speckit/` and archive the legacy feature folders there.
- [x] Review the existing constitution and project context to ensure the OpenSpec planning home reflects current project constraints.
- [x] Create or update OpenSpec capability specs in `openspec/specs/` for the main enduring product areas.
- [x] Create follow-up changes as needed for individual features that should move from historical context into active implementation.
- [x] Validate the resulting planning structure and confirm that the migration preserves intent without changing application behavior.
